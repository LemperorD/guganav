/**:
    ros__parameters:
        port_name : "/dev/ttyACM0"
        baud_rate : 115200 # options: 9600, 19200, 38400, 57600, 115200, 230400

        # 从pc到mcu的速度传输的倍数
        # pc端速度消息单位为m/s，mcu端则为mm/s
        vel_trans_scale : 50.0 # 理论值为1000，将其提取在yaml文件中调试
